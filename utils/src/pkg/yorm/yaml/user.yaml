header:
  version: v1.0
  lang: YORM
models:
  addUser:
    insert into user (user_name, user_age) values (${user.UserName}, ${user.UserAge})
  getUserById:
    - select user_id, user_name, user_age, created_at from user
    - if: user.UserId != 0
      then: where user_id = ${user.UserId}
  updateUser:
   update user set a = 1, b =2
  tes:
    - select * from user where id = ${user.Id}
    - if: user.Name != ""
      then: name = ${user.Name}
    - for: index,user => users
      sep: ","
      then: (${user.Id}, ${user.Name})
    - if: user.Id
      then: id = ${user.Id}
    - group by u.userId
  test:
    - select * from user
    - where id = ${id}